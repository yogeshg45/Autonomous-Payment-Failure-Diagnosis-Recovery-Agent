# Copy this file to .env and fill in your real values.
# .env is already excluded in .gitignore — never commit real keys.

# --------------------------------------------------------------
# LLM provider — Groq (GPT-OSS). Fast and reliable on the free tier.
# --------------------------------------------------------------
LLM_PROVIDER=groq

# Get a free key at https://console.groq.com/keys
# Used by src/llm_provider.py for alert explanations, the Q&A
# chatbot, the panel "Transaction Entry" catalog, and the
# tool-calling recovery agent.
GROQ_API_KEY=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

# Uncomment to override if Groq retires/renames this model later —
# check https://console.groq.com/docs/models first.
# GROQ_MODEL=openai/gpt-oss-120b

# Used by Tab 5 (Real Razorpay test-mode order). Also loaded from
# .env automatically now — see proof/razorpay_real_data_pull.py for
# the schema-validation script that already used these once.
RAZORPAY_KEY_ID=rzp_test_TYQoPB1oKorNmC
RAZORPAY_KEY_SECRET=jh5vFHhExgmFXAodMviFWkxL
